name: CI
on: [push, pull_request]

jobs:
  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend/app
    steps:
      - uses: actions/checkout@v4
      - uses: npm/action-setup@v3
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: './frontend/app/npm-lock.yaml'

      - name: Install dependencies
        run: npm install --frozen-lockfile

      - name: Lint
        run: npm lint

      - name: Test
        run: npm test

      - name: Build Check
        run: npm build